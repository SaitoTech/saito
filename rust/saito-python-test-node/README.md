# saito-python-test-node

This project is the initial interoperability harness for running a Python-driven
Saito node alongside a Rust node.

The first implementation focuses on lifecycle orchestration and testability.
Engine connectivity can be swapped in behind the Python SDK backend interface.

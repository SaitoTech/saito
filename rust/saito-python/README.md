# saito-python

This package is the initial Python SDK scaffold for Saito.

It intentionally starts with a backend protocol boundary so the public Python API
can stabilize before the underlying host-neutral engine implementation lands.

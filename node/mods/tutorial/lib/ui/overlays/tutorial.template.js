module.exports = (app, mod, tutorial_overlay = {}) => {

  return `
    <div class="tutorial-overlay-container'>
      When the Tutorial Overlay is rendered, it will show inside the Saito Overlay
    </div>
  `;

};
